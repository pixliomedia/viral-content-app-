{
  "name": "viral-content-manager",
  "version": "1.0.0",
  "description": "TikTok Content Posting prototype",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "certs": "mkdir -p certs && cd certs && openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem -days 365 -nodes -subj \"/CN=localhost\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "axios": "^1.6.0"
  }
}
